import RegisterForm from "@/app/(public)/login/_components/LoginForm";
import { ReactElement } from "react";

const RegisterPage = (): ReactElement => {
  return <RegisterForm />;
};

export default RegisterPage;
